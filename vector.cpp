#include <vector>

/*
 * Klasa vector - wygodne kozystanie, ale malo wydajna.
 */
int main()
{
	using namespace std;

	vector<int> v;			// bezposrednio po inicjalizacji pozostaje pusta
	vector<int> tab(100);	// po inicjalizacji moze przechowywac 100 elementow

	return 0;
}